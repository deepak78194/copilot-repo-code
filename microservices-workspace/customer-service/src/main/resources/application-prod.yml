micronaut:
  server:
    port: ${SERVER_PORT:8082}

datasources:
  default:
    url: ${DATASOURCE_URL}
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}

logger:
  levels:
    com.example.customer: WARN
